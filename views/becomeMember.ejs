<!DOCTYPE html>

<html>
    <head>
        <title>Members Only</title>
        <link href="/styles.css" rel="stylesheet">
    </head>

    <body>
        <h1>Become a Member</h1>
        
        <% if (locals.user && user.member_status == 'user') {%>
            <form action="/join-member" method="POST">
                <label for="password">What is the password?</label>
                <input type="text" name="password" id="password" required/>
                <p>Hint: One of the most famous Star Wars lines</p>
                <button>Submit Password</button>
            </form>
        <% } else { %>
            <p>You must be logged in and a user to become a member!</p>
        <%}%>
        <a href="/">Back to Home</a>
    </body>
</html>